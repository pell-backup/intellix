syntax = "proto3";
package intellix.oracle;

import "amino/amino.proto";
import "gogoproto/gogo.proto";

option go_package = "intellix/x/oracle/types";

// Params defines the parameters for the module.
message Params {
  option (amino.name) = "intellix/x/oracle/Params";
  option (gogoproto.equal) = true;

  
}